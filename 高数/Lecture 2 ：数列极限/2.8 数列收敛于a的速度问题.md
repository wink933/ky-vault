
##### **定义**
设数列 ${x_n},{y_n}$ 在 $n\to\infty$ 的过程中同时趋于 $a$，记 $u_n=|x_n-a|,\ v_n=|y_n-a|,\ I=\lim_{n\to\infty}\dfrac{u_n}{v_n}$，且当 $n\to\infty$ 时，$u_n$ 和 $v_n$ 都是无穷小量，则有

若 $I=0$，则说明 $x_n$ 的收敛速度比 $y_n$ 的收敛速度快；（高阶）

若 $I=b\ (b\text{为大于零的常数})$，则说明 $x_n$ 的收敛速度是 $y_n$ 的 $\dfrac{1}{b}$ 倍；（同阶）

若 $I=\infty$，则说明 $x_n$ 的收敛速度比 $y_n$ 的收敛速度慢。（低阶）

如：$x_n=\dfrac{1}{n},\ y_n=\dfrac{1}{\sqrt{n}},\ \lim_{n\to\infty}x_n=\lim_{n\to\infty}y_n=0$，故  
$$I=\lim_{n\to\infty}\dfrac{\left|\dfrac{1}{n}-0\right|}{\left|\dfrac{1}{\sqrt{n}}-0\right|}=0,$$  
于是 $x_n$ 比 $y_n$ 收敛于 $0$ 的速度快。

>而数列极限定义为 $\lim_{n\to\infty}x_n=a\Leftrightarrow$ 任意 $\varepsilon>0$，存在 $N>0$，当 $n>N$ 时，恒有 $|x_n-a|<\varepsilon$。



> [!example]+ 例题
> > 例2.17 “存在正整数 $N$，当 $n\ge N$ 时，恒有 $|x_n-a|\le\frac{1}{n}$”，是数列 ${x_n}$ 收敛于 $a$ 的（ ）。
> 
> (A) 充分不必要条件　　　　　　　　　　　　　(B) 必要不充分条件
> 
> (C) 充分必要条件　　　　　　　　　　　　　　(D) 既不充分也不必要条件
> 
> 

> [!success]- 解题步骤
> 解 应选 (A)。
> 
> 如果存在正整数 $N$，当 $n\ge N$ 时，恒有 $|x_n-a|\le\frac{1}{n}$，那么任意的 $\varepsilon>0$，取 $n=\max\{N,\left[\frac{1}{\varepsilon}\right]+1\}$，则
> 
> $|x_n-a|\le\frac{1}{n}<\varepsilon$，所以数列 ${x_n}$ 收敛于 $a$。
> 
> 反之，取 $x_n=a+\frac{1}{\sqrt{n}}$，则数列 ${x_n}$ 收敛于 $a$，但 “存在正整数 $N$，当 $n\ge N$ 时，恒有 $|x_n-a|\le\frac{1}{n}$”
> 
> 并不成立。
> 
> 综上， “存在正整数 $N$，当 $n\ge N$ 时，恒有 $|x_n-a|\le\frac{1}{n}$” 是数列 ${x_n}$ 收敛于 $a$ 的充分不必要条件。

> [!warning]- 易错点
> 想当然的以为这是充要条件

> [!note]- 总结
> 本题与其他题目不太一样，若为充要条件那么$\varepsilon$可以被替换成任意一个不依赖于n的正数，而本题的$\varepsilon$相当于$\frac{1}{n}$，是依赖于n的  
> 可以构造成$u_n=x_n-a\le \frac{1}{n}=\frac{1}{n}+a-a=y_n-a=v_n$ 
> 因此相当于多了个条件，$x_n$的收敛速度要快于$y_n$，这样反过来就不能成立了  
> 参考解析中举出的反例






