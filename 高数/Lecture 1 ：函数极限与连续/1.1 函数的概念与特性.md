---
title: 函数的概念与特性
tags:
  - "#函数"
  - "#数学"
categories:
date: 2026-01-29
---
---


## 1.1.1 函数
### **定义** #函数
> 描述：
> 设x与y是两个变量，D是一个给定的数集，若对于每一个 $x\in D$,按照一定的法则$f$,有一个确定的y值与之对应，则称y为x的函数，记作 $y=f(x)$,称x为自变量，y为因变量，称数集 $D$ 为此函数的定义域,

### **补充**
> 事实上，上述定义的函数是单值函数，若给一个 $x_1$，对应一个 $y_1$；给另外一个$x_2$，对应另外一个 $y_2$，这叫一对一［见图1-1（a）】.若给定 $x_1,x_2  (x_1 \neq x_2)$，它们对应同一个$y$，则称多对一［见图1-1（b）］， 所以函数可以一对一，也可以多对一，这叫单值函数.
但是，若一个$x$对应一个 $y_1$，又对应另一个 $y_2$，也就是一对多，这叫多值函数［见图1-1（c）］， 它不在上述定义中.

![](assets/1.1%20函数的概念与特性/file-20260129114444430.png)

##### **方法**：使用铅直画线法判断是否是单值函数

- 作任意一条铅直线(垂直于x轴)
- 与$f(x)$至多一个交点
## 1.1.2 反函数

##### **定义** #反函数
> 描述：设函数 $y=f(x)$ 的定义域为 $D$，值域为 $R$。如果对于每一个 $y\in R$，必存在唯一的 $x\in D$，使得 $y=f(x)$ 成立，则由此定义了一个新的函数 $x=\varphi(y)$，这个函数称为函数 $y=f(x)$ 的反函数，一般记作 $x=f^{-1}(y)$，它的定义域为 $R$，值域为 $D$。相对于反函数来说，原来的函数也称为直接函数。

##### **理解**
- 严格单调函数必有反函数，比如函数 $y=x^2\ (x\in[0,+\infty))$ 是严格单调函数，故它有反函数 $x=\sqrt{y}$。
- 若把 $x=f^{-1}(y)$ 与 $y=f(x)$ 的图形画在同一坐标系中，则它们完全重合。
>     只是换了位置，没有改变原本的函数关系。
- 只有把 $y=f(x)$ 的反函数 $x=f^{-1}(y)$ 写成 $y=f^{-1}(x)$ 后，它们的图形才关于 $y=x$ 对称。
  >将原本函数中的 $x$ 和 $y$ 交换

##### **结论**
- $$y=f(x)\Leftrightarrow x=f^{-1}(y)$$
- $$y=f(x)\ 与 \ y=f^{-1}(x)\ 关于直线\, y=x\,对称$$
- $$f与f^{-1}互为逆运算$$

##### **方法**：使用水平画线法判断一个函数是否具有反函数

- 首先需要符合铅直画线法，即该函数为多值函数
- 作任意水平直线(垂直于y轴)
- 至多一个交点

##### **重要关系**
- $$f[f^{-1}(x)]=x$$
- $$f^{-1}[f(x)]=x$$
##### **经典例题**
> [!question]+ 题干
> ![](assets/1.1%20函数的概念与特性/file-20260129143429631.png)

> [!tip]- 解题步骤
> ![](assets/1.1%20函数的概念与特性/file-20260129143756500.png)

> [!important]- 总结
> 看到ln要想起对数的性质
> 
>
> $$
\begin{cases}
\ln(ab)=\ln a+\ln b \quad (\text{积变和}),\\[4pt]
\ln\!\left(\dfrac{a}{b}\right)=\ln a-\ln b \quad (\text{商变差}),\\[8pt]
\ln(a^{b})=b\ln a \quad (\text{幂次变倍数}).
\end{cases}
>$$
>要想起共轭性，进行分母有理化


> [!note]+ 函数性质
> （1）函数
> $$
> y=\ln\!\left(x+\sqrt{x^{2}+1}\right)
> $$
> 叫作反双曲正弦函数，其图像如图 1-3(a) 所示。函数
> $$
> y=\dfrac{e^{x}-e^{-x}}{2}
> $$
> 叫作双曲正弦函数，其图像如图 1-3(b) 所示。考生应记住这两个函数的图像。
>
> （2）$y=\dfrac{e^{x}+e^{-x}}{2}$ 叫作双曲余弦函数，其图像如图 1-4 所示，
>
> ![](assets/1.1%20函数的概念与特性/file-20260129150516644.png)
> ![](assets/1.1%20函数的概念与特性/file-20260129150541490.png)



## 1.1.3 复合函数

##### **定义** #复合函数
> 描述：

设函数 $y=f(u)$ 的定义域为 $D_1$，函数 $u=g(x)$ 在 $D$ 上有定义，且 $g(D)\subset D_1$，则由

$$
y=f[g(x)]\ (x\in D)
$$

确定的函数称为由函数 $u=g(x)$ 和函数 $y=f(u)$ 构成的复合函数，它的定义域为 $D$，$u$ 称为中间变量。考生要重点掌握复合的方法。  
> 批注：三层复合函数 $h\{f[g(x)]\}$


##### **理解**
- 内层函数的**值域**是外层函数的**定义域**


## 1.1.4 隐函数

##### **定义**
> 描述：设方程 $F(x,y)=0$，若当 $x$ 取某区间内的任一值时，总有满足该方程的唯一的值 $y$ 存在，则称方程 $F(x,y)=0$ 在上述区间内确定了一个隐函数 $y=y(x)$。

##### **理解**
- 


























