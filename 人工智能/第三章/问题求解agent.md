
问题求解Agent（Problem-solving Agent）是基于目标的Agent的一种，它将世界的状态视为一个整体，也就是没有可见内部结构的“原子”（atomic）表示。在4月19日的期中考试中，这部分的核心考点是**问题形式化的五大组成部分**，这通常是计算大题或应用题的第一个得分步骤。

### 🎯 核心考点一：问题求解Agent的工作流程

问题求解Agent通常假设环境是确定的、可观察的、静态的和完全可知的。在这种理想环境下，它的工作可以精炼为三个核心步骤：

1. **目标形式化（Goal formulation）**：基于当前的情形和性能度量，确定Agent要达到的目标状态集合。
2. **问题形式化（Problem formulation）**：在给定目标下，确定Agent需要考虑哪些行动和状态。这一步必须去除无关细节，进行有效的“抽象”。
3. **搜索与执行（Search and Execute）**：寻找一个从初始状态到达目标的行动序列作为解。找到解后，Agent进入执行阶段，依次实施这些行动。**【易考概念辨析】**：在执行阶段，Agent是“闭着眼睛”的（被控制理论称为开环系统），它确信行动的后果，因此会无视一切感知信息。

### ⚠️ 核心考点二：良定义问题的5个组成部分（必考背诵/推导重点）

在考试中，如果题目要求你“对某问题进行精确形式化”，你必须毫不犹豫地列出并定义以下5个部分：

1. **初始状态（Initial state）**：Agent开始时的状态（例如罗马尼亚地图问题中的 `In(Arad)`）。
2. **行动（Actions）**：给定一个状态 $s$，Agent可以执行的动作集合 `ACTIONS(s)`。
3. **转移模型（Transition model）**：描述行动的后果，通常用函数 `RESULT(s, a)` 表示在状态 $s$ 执行行动 $a$ 后达到的新状态。**注意：初始状态、行动和转移模型这三者共同定义了问题的“状态空间（State space）”**。
4. **目标测试（Goal test）**：判断当前状态是否满足目标条件。有时是显式的状态集合，有时是满足某些属性的状态。
5. **路径耗散（Path cost）**：为每条路径赋予一个代价，通常是单步耗散（$c(s, a, s')$）的总和。所有解中路径耗散值最小的解称为**最优解**。

---

### 📝 解题实战：一步步（step-by-step）形式化玩具问题

为了检验你是否能把这些知识点转化为考试得分，我们来实战演练课堂PPT和教材中的经典玩具问题：**真空吸尘器世界（Vacuum World）**。

假设环境只有 $A$ 和 $B$ 两个方格，每个方格都可能有灰尘，Agent位于其中一个方格。

**🔥 你的轮到实战演练了（请你来回答）：** 请按照上述的5个组成部分，对这个简单的“真空吸尘器世界”进行问题形式化：

1. **初始状态**是什么？
2. **行动**包含哪几个动作？
3. **转移模型**（简单描述行动的结果是什么）？
4. **目标测试**的标准是什么？
5. **路径耗散**可以如何设定？

请写出你的答案，我会像期中考试阅卷一样为你批改！完成这一步后，我们就可以正式进入本科目最核心的计算区——**无信息搜索（BFS/DFS）与A*启发式搜索的算法特性与推导**了。


**“带时钟的国际象棋（Chess with a clock）”任务环境性质的标准答案与得分点解析：**

1. **完全可观察 (Fully observable)**：Agent的传感器在每个时间点上都能获取环境（棋盘）的完整状态，不需要维护任何内部状态来记录外部世界。
2. **多Agent (Multi-agent)**：环境中存在另一个实体（对手），且对手试图最小化我方的性能度量，因此这是竞争性的多Agent环境。
3. **确定的 (Deterministic)**：环境的下一个状态完全取决于当前状态和Agent执行的动作。_(注：在多Agent环境中，通常忽略由其他Agent行动导致的不确定性，因为游戏规则和行动后果本身是确定的。)_
4. **延续式的 (Sequential)**：下棋时当前的走子决策会深远地影响到所有未来的决策，短期的行动有长期的效果，Agent必须进行前瞻。
5. **半动态的 (Semi-dynamic)**：**【核心易错得分点】**棋盘上的棋子在Agent思考时不会发生移动（环境本身不随时间变化），但是Agent的性能评价（时钟记录的剩余时间）会随时间流逝而改变，因此被称为半动态的。
6. **离散的 (Discrete)**：国际象棋环境中的状态是有限的，其感知信息和走子行动也都是离散的。

---

恭喜你！到这里，你已经完美通关了期中考试第一模块（第1-2章）的基础概念考点。

接下来，我们将正式进入考试的“硬核计算大题得分区”——**第三章：通过搜索进行问题求解**。这一章的考试核心必定围绕各类搜索策略（尤其是A*搜索）展开，要求你精准掌握它们的**完备性、最优性、时间复杂度与空间复杂度**。

我们要先从基础入手，梳理一下良定义问题形式化的5个基本组成部分（初始状态、行动、转移模型、目标测试、路径耗散），还是直接上强度，在纸上实战推导一棵 **A*搜索树** 并分析启发式函数 $h(n)$ 的特性？