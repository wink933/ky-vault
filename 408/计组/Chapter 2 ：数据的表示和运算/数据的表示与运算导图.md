# 数制与编码

## 进位计数制及转换
### 采用二进制编码的原因
#### 两个状态使用两个物理器件
#### 布尔
#### 简单
### 进位计数法
#### 进制
#### 基数
## 定点数的编码表示

### 真值
#### 实际数值
### 机器数
#### 将真值符号化

### 定点表示

#### 定点小数
##### 纯小数
#### 定点整数
##### 纯整数
#### 小数点是人为规定的，机器内没有小数点

### 编码方式
#### 原码表示
##### 注意原码是有符号位的
##### 有正零和负零
#### 补码表示
##### 正补与原码相同，负补除符号位取反再加1
##### 模运算，类比于钟表
##### 零表示唯一
##### 相反数的补码相当于全部位取反再加1
##### 变形补码
###### 左符号代表真正的符号位，右符号代表溢出

#### 反码表示
#### 移码表示
##### 用于浮点数的阶码
##### 其表示为对应的补码的符号位取反，在浮点数表示中就不能直接取反了，因为偏置值不同
##### 真值加上偏置值
##### 可直接比较
##### 零表示唯一


## 无符号数

### 数值隐含为非负整数

## 有符号数

### 补码表示
### 一位符号位

### 为什么使用补码表示有符号数
#### 零表示唯一
#### 范围更大
#### 符号位和数值位一同运算

## 整型数据类型
### int
### short
### long
### char
#### 通常按无符号处理

### 整型数据的类型转换
#### 位截断
##### 丢弃高位保留低位

#### 位扩展
##### 零扩展
###### 无符号数
###### 高位补0
##### 符号扩展
###### 有符号数
###### 高位填充符号位

### 强制类型转换
#### 长转短
##### 位截断
#### 相同字长转换
#### 短转长
##### 位扩展

# 运算方法和运算电路
## 加法器

### 一位全加器
#### 三个输入
#### 两个输出
### 半加器
### 串行进位加法器
#### 进位信号逐级传递
#### 位数越多延迟越大
### 并行进位加法器
#### 同时生成所有进位信号
### 带标志加法器

#### 在加法器的基础上增加了额外的逻辑电路用于生成标志位

## 有符号数的加减运算
### 溢出判别方法
#### 采用一位符号位
#### 一位符号位加进位情况
##### 最高位进位和次高位进位异或
#### 双符号位
##### 不同表示溢出
##### 最左边的符号位表示真正的符号位，最右边的符号位表示计算得到的符号位

#### 对于加法运算，符号位相反，不会溢出，符号位相同可能会溢出，正加正产生正溢出，负加负产生负溢出
## 无符号的加减运算
### 溢出判别方法
#### 手算法
##### 是否在此范围内
#### 结合进位cout
##### 加法运算cout=1表示溢出
##### 减法运算cout=0表示溢出
#### 加法时看是否有进位
#### 减法时看是否有借位
#### 可通过sub和cout异或来判别
## 加减运算电路
### 一套电路，两种语意
### 加法运算的原理
#### sub=0，X和Y正常输入
### 减法运算的原理

### 各类标志位的含义
### 无符号数大小比较
#### ZF和CF
#### 若A>B，实际上cout=1，因为比较运算实际上是进行减法运算，而两个数相减可以等价于加上一个模长度，这样肯定会发生进位，但是CF等于0，因此CF可以使用sub和cout异或表示
### 有符号数大小比较
