---

topic: "链表（Linked List）"  
module: "线性表"  
tags:

- "#链表"
    
- "#单链表"
    
- "#双链表"
    
- "#循环链表"
    
- "#头结点"
    
- "#指针操作"  
exam_weight: "高"  
one_liner: "用“指针串接+按需分配”解决顺序表插删搬移与长度不确定的问题，代价是不能随机访问"  
updated: "2026-01-23"
    

---

> [!summary] 🧠 概念卡｜链表（线性表）
> 
> - 核心得分点：**结点=数据域+指针域**，靠指针把线性关系“串起来”，逻辑相邻≠物理相邻
>     
> - 题目一眼识别：频繁**插入/删除**、表长不确定、需要**就地拼接/拆分** → 首选链表
>     
> - 408最常考：**单链表**（带/不带头结点）、**双链表**、**循环链表**；以及基本操作的指针改动
>     
> - 必背性质：链表**不支持按下标O(1)访问**；定位第i个需要从头走 → O(i)
>     
> - 参考：王道线性表章节“链表”常考口径
>     

> [!note] 🎯 为什么需要（只写“问题与动机”，不写定义）
> 
> - 顺序表插删要搬移元素：频繁插删时 **O(n)** 太亏 → 链表用改指针把插删降到“定位后O(1)”
>     
> - 表长未知或波动大：数组需预分配/扩容 → 链表**按需申请/释放**
>     
> - 需要结构化拼接：合并、拆分、反转、删除区间等更适合“指针重连”
>     
> - 允许节点分散存储：适配动态内存场景（代价是指针开销与缓存不友好）
>     

> [!info] 🔗 机制链条（背诵版｜结构清晰）
> 
> - 触发(频繁插删/长度不定/需要拼接拆分/不强调随机访问)
>     
> - → 输入(结点指针head；待插入值x或待删除位置/关键字k；可能含前驱/后继指针)
>     
> - → 核心步骤〔先定位“改指针的切入点”→ 再只改有限条指针完成重连〕
>     
> - → 输出〔更新后的链；返回插入/删除是否成功；可返回被删元素〕
>     
> - → 副作用/代价〔时间：定位O(n)，定位后插删O(1)；空间：每结点多指针域（单链1个、双链2个）〕
>     
> - → 失败/异常分支〔空表；越界(i≤0或i>len+1)；找不到关键字；删除首元/尾元特殊；未保存后继导致断链；内存申请失败〕
>     

> [!tip] 🧭 流程/状态机（伪图示｜画关键分支）
> 
> - 单链表插删的“关键分支”几乎都在：**是否带头结点**、是否在**首元/尾部/中间**、是否需要**前驱**
>     
> - 口诀：**先连新，再断旧；先保存后继，再改当前指针**
>     
> 
> ```text
> 【单链表（带头结点）按位插入到第 i 个位置】
> 前置：p 指向头结点，j=0
> while p != NULL 且 j < i-1:
>   p = p->next; j++
> if p==NULL: 失败(越界/位置非法)
> s = new Node(x); 若失败→失败
> s->next = p->next
> p->next = s
> 
> 【单链表（带头结点）删除第 i 个结点】
> 前置：p 指向头结点，j=0
> while p->next != NULL 且 j < i-1:
>   p = p->next; j++
> if p->next==NULL: 失败(越界)
> q = p->next            (q是待删)
> p->next = q->next      (先重连)
> free(q)
> 
> 【无头结点的“首元插删”分支】
> 插入到表头：s->next=head; head=s
> 删除表头：q=head; head=head->next; free(q)
> ```

> [!abstract] 🧾 最小对比表（只保留能得分的维度）
> 
> |结构|指针域|典型优点|典型缺点|常考操作要点|
> |---|--:|---|---|---|
> |单链表|1|结构简单，插删(定位后)O(1)|找前驱难；不能逆向遍历|删除/插入常需“前驱p”|
> |双链表|2|易找前驱后继，插删更顺手|指针开销更大|插删要改4条指针（注意次序）|
> |循环单链表|1|从任一点出发可遍历一圈；尾连头|判空/判尾要特别约定|常配“尾指针”实现首尾O(1)|
> |带头结点|+1结点|统一首元插删逻辑，减少特判|多1结点空间|空表：head->next==NULL|

> [!question] 🧩 真题命题信号词 → 直接定位
> 
> - “**按位插入/删除第i个**”“**在p结点后插入**” → 单链表指针改动（是否带头结点）
>     
> - “**求长度/查找第i个/查找前驱**” → 遍历计数，时间O(n)；单链找前驱更麻烦
>     
> - “**就地逆置/反转链表**” → 三指针(prev, cur, next) 迭代重连
>     
> - “**合并两个有序链表**” → 双指针+尾插(带头结点易写)，整体O(m+n)
>     
> - “**判环/找中点/倒数第k个**” → 快慢指针（Floyd/双指针技巧）
>     
> - “**删除重复元素/删除区间[a,b]**” → 维护前驱p与当前q，边走边删（注意保存q->next）
>     

> [!warning] ⚠ 易错点清单（带自检）
> 
> -  **头结点 vs 头指针**混了：题目说“带头结点”时，首元是`head->next`
>     
> -  插删顺序错导致断链：是否遵守“**先保存后继，再改指针**”
>     
> -  删除结点没free / free后还用q（野指针）
>     
> -  单链表删除需要前驱：是否错误地直接删`p`而没法改前驱的next
>     
> -  循环链表遍历死循环：终止条件是否用“回到起点”
>     
> -  双链表插删漏改指针：插入/删除至少涉及前驱、当前、后继共4条链接
>     
> -  越界判断不严：`i`从1开始还是0开始？是否和题设一致
>     

> [!success] ✅ 可直接套用的答题骨架（选择/简答/算法题通用）
> 
> - **不变量一句话**：链表的线性次序由指针决定；任何时刻都要保证“从head出发可到达全部结点且无断链”
>     
> - **操作三件套模板**：前置(空表/越界/是否带头结点) → 步骤(定位前驱/保存后继/改指针) → 结果(重连成功+释放/申请)
>     
> - **单链表插入骨架**：定位到`i-1`结点p → `s->next=p->next` → `p->next=s`
>     
> - **单链表删除骨架**：定位到`i-1`结点p → `q=p->next` → `p->next=q->next` → `free(q)`
>     
> - **双链表插入骨架**（在p后插s）：
>     
>     - `s->next=p->next; s->prior=p;`
>         
>     - 若`p->next!=NULL`：`p->next->prior=s`
>         
>     - `p->next=s`
>         
> - **防错口令**：改指针前先“**抓住next**”，插入先连新后断旧，删除先跨过再释放
>     

> [!quote] 🔗 知识网络（双链串题）
> 
> - [[线性表]]：顺序表 vs 链表的取舍（插删 vs 随机访问）
>     
> - [[顺序表]]：插删O(n)搬移；扩容策略与均摊
>     
> - [[头结点与头指针]]：统一边界处理的高频考点
>     
> - [[栈的链式存储（链栈）]]：头插头删=O(1)
>     
> - [[队列的链式存储（链队）]]：带尾指针/循环优化
>     
> - [[快慢指针]]：判环/中点/倒数第k个
>     
> - [[链表逆置]]：三指针重连模型
>     
> - [[归并有序链表]]：双指针+尾插的模板题
>