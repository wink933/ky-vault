
1. $x\in D$




2. $x_1 \neq x_2$
3. $x_1$
4. $y=f^{-1}(x)$
5. $\{\, f(x) \mid x \in D \,\}$
	说明：- 花括号：`\{` 和 `\}`
    
- “属于”：`\in`
    
- 条件分隔符：推荐用 `\mid`（比 `|` 更像数学里的“使得/满足”）
    
- `\,` 是小空格，让排版更像教材
	- 
